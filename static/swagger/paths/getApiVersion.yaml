get:
  tags:
    - "common"
  summary: "取得 API 最新版本號"
  description: "取得 API 最新版本號"
  servers:
    - url: '/api'
  responses:
    '200':
      description: "successful operation"
      content:
        application/json:
          schema:
            $ref: '../schemas/common.yaml#/VersionResult'
    '403':
      description: "Invalid status value"
      content:
        application/json:
          schema:
            $ref: '../schemas/common.yaml#/DefaultFailureResult'
          